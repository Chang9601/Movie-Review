<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.chang.recmv.mapper.RevMapper">
	<insert id="writeRev">
		insert into recmv_rev(userNum, movieNum, id, movie, image, title, rating, content, createDate)
		values(#{userNum}, #{movieNum}, #{id}, #{movie}, #{image}, #{title}, #{rating}, #{content}, now())
	</insert>
	
	<select id="getRevs" resultType="Rev">
		select * from recmv_rev
		order by createDate desc
		limit #{beginNumOfItems}, #{numOfItemsPerPage}		
	</select>
	
	<select id="getNumRevs" resultType="int">
		select count(*)
		from recmv_rev
	</select>

	<select id="readRev" resultType="Rev">
		select * from recmv_rev
		where num = #{num}
	</select>
	
	<update id="updateRev">
		update recmv_rev 
		set title = #{rev.title}, content = #{rev.content}, rating = #{rev.rating}, updateDate = now()
		where num = #{num}
	</update>
	
	<delete id="deleteRev">
		delete from recmv_rev
		where num = #{num}
	</delete>
	
	<select id="readNum" resultType="int">
		select num from recmv_user
		where id = #{id}
	</select>
	
	<select id="readId" resultType="string">
		select id from recmv_user
		where num = #{num}
	</select>
	
	<select id="readMovie" resultType="Movie">
		select * from recmv_movie
		where num = #{num}
	</select>	
</mapper>